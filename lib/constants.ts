// The gradebook's subject list lives in the subjects TABLE per school
// (migration 0035) — there is deliberately no hard-coded copy here.
export const TERMS = ["Term 1", "Term 2", "Term 3"] as const;
